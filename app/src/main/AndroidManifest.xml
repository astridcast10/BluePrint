<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_blueprint_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_blueprint_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BluePrint">

        <activity
            android:name="com.uth.gestionusuarios.activities.DetalleUsuarioActivity"
            android:exported="false" />

        <activity
            android:name="com.uth.gestionusuarios.activities.GuardarUsuarioActivity"
            android:exported="false" />

        <activity
            android:name="com.uth.gestionusuarios.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>